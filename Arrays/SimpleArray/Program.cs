string[] cars = { "bmw", "mersedes", "volvo", "ford" }; // Initialize a new array containing string

// Create a for loop with elements of the 'cars' array
for (int index = 0; index < cars.Length; index++)
{
    Console.WriteLine($"Car Name: {cars[index]}");
}